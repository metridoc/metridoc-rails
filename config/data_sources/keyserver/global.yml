---
# Keyserver data source configuration
#
# Production: drop events.csv and sessions.csv into import_folder, then run:
#   rake import -- --config_folder keyserver
#

institution_code: 'UPENN'
import_folder:    '/tmp/keyserver'
