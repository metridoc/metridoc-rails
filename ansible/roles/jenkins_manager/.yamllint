extends: default

rules:
  braces:
    max-spaces-inside: 1
    level: error
  brackets:
    max-spaces-inside: 1
    level: error
  document-start:
    ignore: |
      docker-compose.yml
  line-length: disable
  truthy: disable
